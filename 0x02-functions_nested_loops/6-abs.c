#include "main.h"

/**
 * *main-compute the absolute value of an integer
 * *
 * *Return: The absolute value of an integer
 */

int _abs(int x)
{
	if (x > 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
